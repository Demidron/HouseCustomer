import { ValidStatusDirective } from './valid-status.directive';

describe('ValidStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidStatusDirective();
    expect(directive).toBeTruthy();
  });
});
